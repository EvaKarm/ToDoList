<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List</title>
  </head>
  <body>
    <!-- Переменная ejs kindOfDay, синтаксис ejs -->
    <!-- <h1>It is a <%= kindOfDay %> !</h1> -->

    <% if (kindOfDay === "Saturday" || kindOfDay === "Sunday") { %>
    <h1 style="color: purple"><%= kindOfDay %> ToDo List</h1>
    <% } else { %>
    <h1 style="color: blue"><%= kindOfDay %> ToDo List</h1>
    <% } %>
    <!-- Scriptlet' tag, for control-flow, dif. tags for dif. things, on each line -->
  </body>
</html>
